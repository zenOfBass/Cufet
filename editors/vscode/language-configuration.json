{
  // C-style, with one difference: the block form NESTS, as it does in Rust and Swift.
  "comments": {
    "lineComment": "//",
    "blockComment": ["/*", "*/"]
  },

  "brackets": [
    ["(", ")"],
    ["{", "}"],
    // Foreign source — [ ... ]. Pairs nest inside one, so bracket matching is what a reader needs
    // most there: a C subscript is the commonest reason an axiom has more than one pair.
    ["[", "]"]
  ],

  "autoClosingPairs": [
    { "open": "/*", "close": " */" },
    { "open": "(", "close": ")" },
    { "open": "{", "close": "}" },
    // Not inside one: an axiom holds C, where a '[' is far more often a subscript being typed
    // than a pair being opened, and auto-closing it lands a ']' that ends the axiom.
    { "open": "[", "close": "]", "notIn": ["string", "comment"] },
    // No pair for ' — the possessive 's would auto-close into nonsense on every field access.
    { "open": "\"", "close": "\"", "notIn": ["string", "comment"] }
  ],

  "surroundingPairs": [
    ["(", ")"],
    ["{", "}"],
    ["\"", "\""]
  ],

  // Hyphens are identifier characters in Cufet: 'add-edge' is ONE name, not two words with a
  // dash between them. Without this, double-clicking it selects only 'add', word-wise cursor
  // movement stops inside it, and selecting a name to rename picks up half of one.
  "wordPattern": "[A-Za-z][A-Za-z0-9]*(?:-[A-Za-z0-9]+)*|\\d+(?:\\.\\d+)?",

  // A block opens with a trailing ':' and closes with 'Done.' or 'Until ...'. 'Otherwise'
  // closes the arm above it and opens its own, so it dedents on the way in, and its own
  // trailing ':' indents the body back out again.
  "indentationRules": {
    "increaseIndentPattern": ":\\s*$",
    "decreaseIndentPattern": "^\\s*(?i:done|until|otherwise)(?![\\w-])"
  },

  "onEnterRules": [
    {
      "beforeText": ":\\s*$",
      "action": { "indent": "indent" }
    }
  ]
}
